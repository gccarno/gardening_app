<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Release builds: HTTPS only (the cloud backend is HTTPS).
         Debug builds use src/debug/res/xml/network_security_config.xml, which
         permits cleartext for the emulator (10.0.2.2) and home-LAN IPs. -->
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
